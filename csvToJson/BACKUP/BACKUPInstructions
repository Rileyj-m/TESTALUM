In the event that the logic of this csv handling fails, we can include the location of where to put a manually formatted json.
we can include a link to format the json from a csv, and all they have to do is place the formatted json in the backup location.